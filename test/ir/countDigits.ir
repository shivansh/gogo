func, main
declStr, nStr, "Enter n: "
printStr, nStr
scanInt, n
=, count, 0
label, while
ble, exit, n, 0
/, n, n, 10
+, count, count, 1
jmp, while
label, exit
declStr, str, "Number of digits in n: "
printStr, str
printInt, count, count
ret,
